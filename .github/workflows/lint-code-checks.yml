###############################################################
#
## Copyright (©) 2024-2025 David H Hoyt. All rights reserved.
##                 https://srd.cx
##
## Last Updated:09-NOV-2025 at 1100Z by David Hoyt (©)
#
## Intent: linter * code checks
#
## TODO: 
#
#
###############################################################

name: "lint"

permissions:
  contents: read
  pull-requests: read

on:
  # Allow manual trigger
  workflow_dispatch:

  # Allow other workflows to call this
  workflow_call:
    inputs:
      ubuntu-version:
        description: "Ubuntu version for runner"
        required: false
        default: "ubuntu-latest"
        type: string

  # Auto-trigger on PRs
  pull_request:
    branches:
      - master
      - test
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  build-linux:
    name: Lint on ${{ inputs.ubuntu-version || 'ubuntu-latest' }}
    runs-on: ${{ inputs.ubuntu-version || 'ubuntu-latest' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Git anonymous identity
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cppcheck clang-format build-essential cmake git pkg-config \
            gcc g++ clang clang-tools libpng-dev libxml2 libxml2-dev libtiff-dev \
            nlohmann-json3-dev libwxgtk3.2-dev wx-common python3 python3-pip curl git llvm zsh

      - name: Lint & code checks
        run: |
          mkdir -p lint_reports
          echo "Placeholder for cppcheck..."
          echo "Running example cppcheck..."
          echo "Maintainer runs lint checks manually...."     
          echo "Maintainer runs lint checks manually..." > lint_reports/cppcheck.txt
          # cppcheck --enable=warning --quiet --std=c++17 \
          #  --output-file=lint_reports/cppcheck.txt \
          #  Tools/CmdLine/*.cpp || true

      - name: Upload cppcheck report
        uses: actions/upload-artifact@v4
        with:
          name: cppcheck-report
          path: lint_reports/cppcheck.txt

      - name: Generate GitHub summary
        if: always()
        run: |
          echo "## 🧹 Lint Report Summary" >> $GITHUB_STEP_SUMMARY
          echo "### cppcheck results:" >> $GITHUB_STEP_SUMMARY
          if [ -s lint_reports/cppcheck.txt ]; then
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            tail -n 30 lint_reports/cppcheck.txt >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No cppcheck warnings or errors detected." >> $GITHUB_STEP_SUMMARY
          fi
