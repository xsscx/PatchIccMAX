###############################################################
#
## Copyright (©) 2025 International Color Consortium. 
##                 All rights reserved. 
##                 https://color.org
#
#
## Intent: Static code analysis and linting (cppcheck, clang-tidy, clang-format) for PR validation
#
## Last Updated: 06-DEC-2025 1400Z by David Hoyt 
##               TEST job Failures
## TODO: Push binary releases, tags etc.. 
#
## Comment: Known good and working from PatchIccMax
#
#
#
#
#
###############################################################

name: "ci-pr-lint"

permissions:
  contents: read
  pull-requests: read

on:
  # Allow manual trigger
  workflow_dispatch:

  workflow_call:
    inputs:
      ubuntu-version:
        description: "Ubuntu version for runner"
        required: false
        default: "ubuntu-latest"
        type: string

jobs:
  build-linux:
    name: Lint on ${{ inputs.ubuntu-version || 'ubuntu-latest' }}
    runs-on: ${{ inputs.ubuntu-version || 'ubuntu-latest' }}
    timeout-minutes: 30
    steps:

      - name: Validate ubuntu-version input
        shell: bash
        run: |
          set -euo pipefail
          case "${{ inputs.ubuntu-version }}" in
            ubuntu-latest|ubuntu-22.04|ubuntu-24.04|"")
              ;;
            *)
              echo "ERROR: Invalid runner: '${{ inputs.ubuntu-version }}' — blocked." >&2
              exit 1
              ;;
          esac

      - uses: actions/checkout@c2d88d3ecc89a9ef08eebf45d9637801dcee7eb5
        with:
          fetch-depth: 0
          # Disable unsafe fetch options
          persist-credentials: false

      - name: Set up Git anonymous identity
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"

      - name: UCI Gate 1 - Verify remote origin URL (opt out of malicious git configs)
        shell: bash
        env:
          BASH_ENV: /dev/null
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global credential.helper ""
          origin_url=$(git remote get-url origin || true)
          echo "Remote origin: $origin_url"
          # Enforce that origin points to the expected GitHub repository for this workflow run
          expected="https://github.com/${{ github.repository }}.git"
          # Allow both HTTPS and git@ forms if needed
          if [[ "$origin_url" != "$expected" && "$origin_url" != "${expected%.git}" && "$origin_url" != "git@github.com:InternationalColorConsortium/iccDEV.git" ]]; then
            echo "Origin URL mismatch: expected $expected, ${expected%.git}, or git@github.com:InternationalColorConsortium/iccDEV.git, got $origin_url" >&2
            exit 1
          fi

      - name: Install Linux dependencies
        shell: bash
        env:
         BASH_ENV: /dev/null
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global credential.helper ""
          
          # Clear the in-shell GITHUB_TOKEN
          unset GITHUB_TOKEN || true

          sudo apt-get update
          sudo apt-get install -y cppcheck clang-format build-essential cmake git pkg-config \
            gcc g++ clang clang-tools libpng-dev libxml2 libxml2-dev libtiff-dev \
            nlohmann-json3-dev libwxgtk3.2-dev wx-common python3 python3-pip curl git llvm zsh

       - name: UCI Gate 2 - Detect modified source files
         id: uci_gate2_diff
         shell: bash
         env:
           BASH_ENV: /dev/null
         run: |
           set -euo pipefail
           git config --global --add safe.directory "$GITHUB_WORKSPACE"
           git config --global credential.helper ""
           
           # Clear the in-shell GITHUB_TOKEN
           unset GITHUB_TOKEN || true

           # Display commit info safely
           git show --stat --pretty=format:"Commit: %H%nAuthor: %an%nDate: %ad%n" HEAD

           # --- SAFE base_ref handling (sanitize + validate) ---
           raw_base_ref="${{ github.base_ref }}"
           base_ref="$(printf '%s' "${raw_base_ref}" | tr -cd '[:alnum:]._-/')"

           if [ -z "${base_ref}" ]; then
             echo "ERROR: base_ref empty or invalid after sanitization" >&2
             exit 1
           fi

           case "${base_ref}" in
             master) ;;   # allowed
             *)
               echo "ERROR: Unsupported target branch '${base_ref}'. Only 'master' allowed." >&2
               exit 1
               ;;
           esac

           # --- filename list (safe pipeline) ---
           git diff --name-only "origin/${base_ref}"...HEAD \
             | tr -cd '[:alnum:]./_-\n' \
             | grep -E '\.(cpp|cxx|cc|h|hpp)$' \
             | tr '\n' '\0' > changed_files.txt

           changed_file_count="$(xargs -0 -n1 < changed_files.txt | wc -l)"

           if [ "${changed_file_count}" -eq 0 ]; then
             echo "no_src_changes=true" >> "$GITHUB_OUTPUT"
           else
             echo "no_src_changes=false" >> "$GITHUB_OUTPUT"
           fi

      - name: Lint & code checks
        if: ${{ steps.diff.outputs.no_src_changes == 'false' }}
        shell: bash
        env:
         BASH_ENV: /dev/null
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global credential.helper ""
          
          # Clear the in-shell GITHUB_TOKEN
          unset GITHUB_TOKEN || true
  
          mkdir -p lint_reports Build
  
          echo "Running cppcheck on modified source files..."
  
          # --- SECURITY: null-delimited xargs ---
          : > lint_reports/cppcheck.txt
          xargs -0 cppcheck \
            --enable=warning,performance,portability \
            --std=c++17 \
            --output-file=lint_reports/cppcheck.txt \
            < changed_files.txt || true
  
          # Prepare clang-tidy
          cd Build
          cmake Cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
          make
          cd ..
  
          # --- clang-tidy ---
          : > lint_reports/clang_tidy.txt
          xargs -0 run-clang-tidy \
            -p Build \
            -checks='modernize-*,readability-*,cppcoreguidelines-*,clang-analyzer-core.*,clang-analyzer-security.*,clang-analyzer-alpha.core.*,clang-analyzer-alpha.security.*' \
            < changed_files.txt \
            | tee lint_reports/clang_tidy.txt || true

      - name: Skip lint (no source changes)
        if: ${{ steps.diff.outputs.no_src_changes == 'true' }}
        shell: bash
        env:
         BASH_ENV: /dev/null
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global credential.helper ""
          
          # Clear the in-shell GITHUB_TOKEN
          unset GITHUB_TOKEN || true
          mkdir -p lint_reports
          echo "No .cpp/.h changes detected; skipping lint." > lint_reports/cppcheck.txt
          : > lint_reports/clang_tidy.txt

      - uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4
        with:
          name: lint-reports
          path: lint_reports/


      - name: Generate GitHub summary
        if: always()
        shell: bash
        env:
         BASH_ENV: /dev/null
        run: |
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global credential.helper ""
          
          # Clear the in-shell GITHUB_TOKEN
          unset GITHUB_TOKEN || true
          echo "## 🧹 Lint Report Summary" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.diff.outputs.no_src_changes }}" == "true" ]; then
            echo "🟦 Skipped — no modified source files detected." >> $GITHUB_STEP_SUMMARY
          elif [ -s lint_reports/cppcheck.txt ]; then
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            tail -n 30 lint_reports/cppcheck.txt >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No cppcheck warnings or errors detected." >> $GITHUB_STEP_SUMMARY
          fi
